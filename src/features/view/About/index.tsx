import { Container } from "@/components";
import { Intro } from "@/features/landing";

const About = () => {
  return (
    <Container>
      <Intro />
    </Container>
  );
};

export default About;
